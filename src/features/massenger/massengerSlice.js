
import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const massengerSlice = createSlice({
    name: "massenger",
    initialState,
    reducers: {},
});

export const {} = massengerSlice.actions;
export default massengerSlice.reducer;